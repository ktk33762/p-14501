spring:

  application:
    name: controller-bean-aop-2508
  profiles:
    active: dev
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
  datasource:
    url: jdbc:mysql://localhost:3307/db_dev
    username: root
    password: root123414
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: false

logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE